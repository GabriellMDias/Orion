{
  "tables": {
    "approval_requests": {
      "owner": "Approval Request",
      "classification": "INTERNAL",
      "description": "Canonical current state of a human-owned Approval Request, from draft through a terminal decision or cancellation.",
      "lifecycle": "Created in DRAFT; retained through terminal state. No automatic deletion or legal retention duration is defined; production retention remains a separate decision.",
      "columns": {
        "id": {
          "description": "Backend-generated immutable UUID that is both the primary key and public resource identifier.",
          "classification": "INTERNAL"
        },
        "creator_id": {
          "description": "Immutable provider-independent Orion principal UUID of the human who owns the request; supplied by the trusted authentication boundary.",
          "classification": "INTERNAL"
        },
        "title": {
          "description": "Owner-authored concise purpose of the approval request; editable only in DRAFT.",
          "classification": "INTERNAL"
        },
        "description": {
          "description": "Optional owner-authored supporting context; editable only in DRAFT.",
          "classification": "INTERNAL",
          "nullMeaning": "No supporting context supplied."
        },
        "status": {
          "description": "Current lifecycle state; APPROVED, REJECTED, and CANCELLED are terminal.",
          "classification": "INTERNAL"
        },
        "version": {
          "description": "Optimistic concurrency token, initialized to one and incremented exactly once per successful edit or transition.",
          "classification": "INTERNAL",
          "unit": "successful writes"
        },
        "rejection_reason": {
          "description": "Reviewer-authored reason for rejection, present exactly when status is REJECTED.",
          "classification": "INTERNAL",
          "nullMeaning": "The request has not been rejected."
        },
        "idempotency_key": {
          "description": "Opaque caller-supplied creation intent key, unique within creator_id and retained with the request.",
          "classification": "INTERNAL"
        },
        "intent_fingerprint": {
          "description": "SHA-256 fingerprint of the canonical title and description creation intent; detects key reuse for different content.",
          "classification": "INTERNAL"
        },
        "created_at": {
          "description": "PostgreSQL-clock absolute creation instant, serialized as UTC RFC 3339; does not constitute audit history.",
          "classification": "INTERNAL",
          "unit": "UTC instant, millisecond precision"
        },
        "updated_at": {
          "description": "PostgreSQL-clock absolute instant of the latest successful write; not a concurrency token or audit history.",
          "classification": "INTERNAL",
          "unit": "UTC instant, millisecond precision"
        }
      },
      "objects": {
        "approval_requests_creator_key_unique": "Prevents two requests for one owner and creation intent key, including racing inserts.",
        "approval_requests_mine_page_idx": "Supports authorized owner pagination by creation time and ID.",
        "approval_requests_review_page_idx": "Supports submitted reviewable pagination before applying the non-owner predicate.",
        "approval_requests_version_positive": "Rejects invalid concurrency versions.",
        "approval_requests_title_nonblank": "Rejects blank titles even if a caller bypasses the API.",
        "approval_requests_description_nonblank": "Optional description cannot be whitespace only.",
        "approval_requests_rejection_reason_state": "Requires one nonblank reason exactly for a rejected request.",
        "approval_requests_idempotency_key_nonblank": "Creation keys must use the documented opaque ASCII key alphabet and cannot be blank.",
        "approval_requests_fingerprint_sha256": "Fingerprint is exactly lowercase SHA-256 hex."
      }
    }
  },
  "enums": {
    "ApprovalRequestStatus": "Five-state request lifecycle: DRAFT, SUBMITTED, APPROVED, REJECTED, CANCELLED."
  }
}
